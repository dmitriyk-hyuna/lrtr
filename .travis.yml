language: php

php:
  - 5.6

before_script:

  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - chmod -R 755 storage

script:
  - php artisan config:clear
  - vendor/bin/phpunit