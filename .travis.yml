language: php

php:
  - 7.0

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - composer update
  - chmod -R 755 storage
  - php artisan key:generate
script:
  - vendor/bin/phpunit